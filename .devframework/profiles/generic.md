# Generic profile

No runtime, branch strategy, output path or deployment target is assumed.
Agree project specifics in PROJECT.md before using a live environment.
Keep the existing branch; build/test only isolated outputs. Configure commands before
claiming the project is ready. Libraries and documentation-only projects can declare
build not applicable with a reason; tests still need an actual command.
